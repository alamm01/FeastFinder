<!-- Loop over each object in the projects array -->
{{!-- {{#each projects as |project| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/project/{{project.id}}">{{project.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div>
{{/each}} --}}
<div class="row align-items-center">

  <div class="col-md-6 col-sm-12 homepage-background">
  </div>
  <div class="col-md-6 col-sm-12">
    <h2 class="text-white">
      Welcome to feastFinder
    </h2>
    <p class="text-white">Schedule your feast with friends and enjoy the seemless experience all in one app. Through our
      app, you can easily make and cancel your fine dining reservations. After you schedule your reservation, you will
      be sent a confirmation email so you can have the peace of mind that your reservation has been properly placed.
    </p>
  </div>
</div>